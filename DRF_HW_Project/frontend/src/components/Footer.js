import React from 'react'
import CustomHeader from "./Header";

const CustomFooter = ({footer_data}) => {
    return (
        <div>
            <h1>There could be cool footer {footer_data}</h1>
        </div>
    )
}

export default CustomFooter